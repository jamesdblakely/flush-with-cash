// Plain JavaScript only: no sprites, browser APIs, or Phaser dependency.
// Milestone 0 has no economy or day simulation yet.
export function createInitialState() {
  return { phase: 'foundation' };
}
